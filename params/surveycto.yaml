auth_file: scto_auth.txt
environments:

  # >>>>> THIS IS SPARTA!
  - name: prod
    streams:
      # - id: bailiwicks
      #   sync_mode: overwrite
      # - id: hh_listing_example_1
      #   sync_mode: deduped
  # <<<<< THIS IS SPARTA!

  - name: dev
    streams:
      - id: nepal_mch_ccp_session_quality_assessment_tool
        sync_mode: deduped
      # - id: hh_listing_example_1
      #   sync_mode: deduped
